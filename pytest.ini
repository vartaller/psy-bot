[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    db: requires TEST_DATABASE_URL to be set (skipped otherwise)
filterwarnings =
    ignore::DeprecationWarning
